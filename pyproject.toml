[project]
name = "mineru-pdf2md"
version = "0.1.0"
description = "Windows helper for converting PDF files to Markdown with MinerU."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
